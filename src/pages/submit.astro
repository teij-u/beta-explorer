---
import Layout from '../layouts/Layout.astro';
---
<Layout title="新しいスポットを投稿 | β-Explorer">
  <div class="bg-white dark:bg-zinc-800 rounded-xl shadow-lg p-8 md:p-12 my-8 max-w-2xl mx-auto">
    <h1 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-zinc-200 mb-6">
      新しい穴場スポットを投稿する
    </h1>
    <p class="mb-8 text-gray-600 dark:text-zinc-400">
      あなたが見つけた、まだ知られていない魅力的な場所を教えてください。投稿内容は運営が確認した後、サイトに掲載させていただきます。
    </p>

    {/* Netlify Formsが認識するための特別なフォーム */}
    <form name="spot-submission" method="POST" data-netlify="true" action="/thanks">
      <div class="space-y-6">
        <div>
          <label for="spotName" class="block mb-2 font-semibold text-gray-700 dark:text-zinc-300">スポット名</label>
          <input type="text" name="spotName" id="spotName" required class="w-full p-3 border-2 border-gray-200 rounded-lg bg-white dark:bg-zinc-700 dark:border-zinc-600 dark:text-white">
        </div>
        <div>
          <label for="address" class="block mb-2 font-semibold text-gray-700 dark:text-zinc-300">住所・場所</label>
          <input type="text" name="address" id="address" required class="w-full p-3 border-2 border-gray-200 rounded-lg bg-white dark:bg-zinc-700 dark:border-zinc-600 dark:text-white">
        </div>
        <div>
          <label for="description" class="block mb-2 font-semibold text-gray-700 dark:text-zinc-300">説明・おすすめポイント</label>
          <textarea name="description" id="description" rows="5" required class="w-full p-3 border-2 border-gray-200 rounded-lg bg-white dark:bg-zinc-700 dark:border-zinc-600 dark:text-white"></textarea>
        </div>
      </div>
      <div class="mt-8">
        <button type="submit" class="w-full bg-blue-600 text-white font-bold py-3 px-6 rounded-lg hover:bg-blue-700 transition-transform transform hover:-translate-y-1 shadow-lg">
          投稿を送信する
        </button>
      </div>
    </form>

  </div>
</Layout>